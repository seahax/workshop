import type { Action } from '../action.ts';
import type { State } from '../state.ts';

export function characterMove(state: State, action: Action): State {
  void state;
  void action;
  throw new Error('Not implemented');
};
