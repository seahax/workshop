const LOCATION = '({{line}}:{{column}})';

export const FUNCTION_PARAMS = `Wrap function parameters to reduce line length ${LOCATION}.`;
export const IMPORTS = `Wrap imports to reduce line length ${LOCATION}.`;
export const EXPORTS = `Wrap exports to reduce line length ${LOCATION}.`;
export const OBJECT_PROPS = `Wrap object properties to reduce line length ${LOCATION}.`;
export const ARRAY_ELEMENTS = `Wrap array elements to reduce line length ${LOCATION}.`;
export const TERNARY_BRANCHES = `Wrap ternary branches to reduce line length ${LOCATION}.`;
export const UNION_TYPES = `Wrap union types to reduce line length ${LOCATION}.`;
export const CHAIN = `Wrap chained method calls to reduce line length ${LOCATION}.`;
